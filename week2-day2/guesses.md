# Exercise 2

## Task 2 — Guess

I guessed that the "Borrow selected" button would throw an error when clicked.

I think this will happen because `handleBorrowClick` is not bound in the constructor, unlike `handleFilterClick`.


# Exercise 3

## Task 2 — Guess

I guessed that importing the default Header export as Banner would work, but it could cause confusion because the imported name does not match the actual component name.

I guessed that importing Sidbar from the named Sidebar export would fail immediately because the exported name is Sidebar and the import contains a typo.
